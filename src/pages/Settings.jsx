2つ対応します。

1. **Myセット作成時にサジェスト付き料理入力**（テキスト入力→候補→選択で食材も自動セット）
2. **設定画面の旧テンプレートセクションを削除**
3. **Myセット選択時に食材も取得**（今は `ings:[]` で空になっている）

旧テンプレートのJSXはすでに削除済みです。残っているのはロジック部分の不要なstate/関数だけです。今回の主な修正はMySetManagerにサジェストを追加することです。

Settings.jsxのMySetManagerを丸ごと書き直します。

`src/pages/Settings.jsx` だけ差し替えてください。

---

## 変更内容

**セット作成画面にサジェスト検索を追加**
料理名を入力すると献立入力ページと同じようにDB候補＋Gemini AIの候補が出ます。候補をタップすると**食材も一緒に登録**されます。これでセットを使って献立に追加したとき、食材が買い物リストに反映されます。

**旧テンプレートセクション（朝/昼/夜タブ）は完全削除**
設定画面はMyセットのみになりました。

**セット一覧に食材のプレビューを追加**
展開するとどの料理にどんな食材があるかが確認できます。